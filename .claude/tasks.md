# Task Progress — Expense Reporter

**Last Updated:** 2026-06-09 (session 27)
**Active Layer:** Workbook Mapping
**Full history:** `.claude/session-log.md` (session log), `docs/archive/` (Desktop-era docs)

---

## Pre-History Summary (Claude Desktop — complete)

All work below was done in Claude Desktop before Claude Code was set up for this repo.

- **Phases 1–11:** Full CLI built — CSV parser, Excel writer, category resolver, batch import, Cobra CLI
- **Tests:** 190+ tests, table-driven, all passing
- **Commands:** `add` (single expense), `batch` (CSV import), `version`
- **Version:** v2.1.0
- **Classification analysis:** Auto-categorization of historical expenses using LLM feature extraction
  Results in `data/classification/` — algorithm docs, training data (gitignored), parameters

---

## Two-Repo Workflow

- **This repo** (`~/workspaces/expenses/code/`): Layer 5 feature work (classify, auto, batch-auto commands)
- **LLM repo** (`/mnt/i/workspaces/llm/`): MCP thin wrapper (5.8), ollama-bridge MCP server, personas
- **Scaffolding template:** `/mnt/i/workspaces/llm/docs/scaffolding-template.md`

---

## Layer 5: Expense Classifier

**Goal:** Local model classifies expenses, auto-inserts into Excel via expense-reporter Go tool.
**Context:** `/mnt/i/workspaces/llm/docs/vision/expense-classifier-vision.md` (full vision + iterative plan)
**Data inventory:** `/mnt/i/workspaces/llm/docs/vision/expense-classifier-data-inventory.md`
**Classification data:** `data/classification/` (this repo)

### Pre-work — COMPLETE (LLM repo sessions 32–35)
- [x] **5.0a** ollama-bridge JSONL logging: call logs to `~/.local/share/ollama-bridge/calls.jsonl`
- [x] **5.0b** CLAUDE.md in LLM repo: Layer 5+ local-model-first instruction (ACCEPTED/IMPROVED/REJECTED)
- [x] **5.0c** Model audit: qwen2.5-coder:14b, qwen3:8b-q8_0, qwen3:30b-a3b pulled; personas created
- [x] **5.0d** Multi-model comparison tooling: `run-compare-models.sh` + `run-record-verdicts.sh`
- [x] **5.0e** Fix `think: false` in ollama_client.py: top-level payload, not options{}. 82% token reduction.
- [x] **5.0f** num_ctx 16384→10240 in go-qwen25c14.Modelfile. No OOM.
- [x] **5.0g** my-java-q25c14 persona created (qwen2.5-coder:14b, Java 21 + Spring Boot 3.x)
- [x] **5.0h** Scaffolding bootstrap: this repo now has .claude/ tools, CLAUDE.md, index.md (session 1)

### Layer 5 Tasks (next)
- [x] **5.1** Port training data into expense-reporter: verify `feature_dictionary_enhanced.json` + `training_data_complete.json` are in `data/classification/`; document their JSON format in index.md
- [x] **5.2** `classify` command: 3-field input (date, description, amount) → Ollama HTTP → structured JSON → top-N subcategories with confidence scores
- [x] **5.3** `auto` command: classify + insert into Excel if HIGH confidence (≥0.85), else print candidates
- [x] **5.4** `batch-auto` command: classify a CSV file → classified.csv (HIGH confidence) + review.csv (LOW confidence)
- [X] **5.5** Classification feedback logging: `classifications.jsonl` — `{id, item, date, value, predicted_*, actual_*, confidence, model, status, 
timestamp}` appended on insert (status: confirmed/corrected/manual). Plan: `.claude/plans/polished-knitting-simon.md`
- [x] **5.6** Expense persistence: hash ID (sha256[:12] of normalized item+date+value), `expenses_log.jsonl` appended on each insert
- [x] **5.7** Few-shot injection: keyword pre-match against training data, inject top-K examples into classify prompt
- [x] **5.8** MCP thin wrapper (lives in THIS repo at `mcp-server/`, not LLM repo): `classify_expense` + `add_expense` tools, calls Go binary as subprocess. `auto_add` dropped by design — see `mcp-server/.memories/KNOWLEDGE.md` "Two Tools, Not Three" (2026-03-27).

### Deferred Technical Debt

- [ ] **TestBatchAuto_SameYearInstallmentsExpanded — reduce scope to structural validation**: The current test uses a real workbook insert to verify installment expansion, but this is fragile — the classifier may return a category name instead of a subcategory name (e.g. "Saúde" instead of "Academia"), causing a resolution error independent of installment logic. The installment expansion is already covered by `workflow_test.go` unit tests. Better acceptance test: use `--dry-run` and assert the classified.csv has the expense with installment notation preserved in the `value` column, or just assert structural validity (`classifiedAndReviewFilesProduced()`). Avoids dependency on classifier subcategory quality for a test whose purpose is installment mechanics.

- [x] **config reader: replace runtime.Caller with os.Executable** — Fixed session 4 (commit 405953e). `internal/config/config.go` now uses `os.Executable()` to resolve `config/config.json` relative to the binary location.

### Deferred — Retrieval Pipeline Evolution (from 5.7 planning, session 10)

These build on the keyword matching layer delivered in 5.7. Each is a separate cascade
layer — see `data/classification/retrieval-strategy.md` for the full pipeline design.

- [ ] **5.R1** TF-IDF retrieval layer: second cascade layer for multi-word similarity matching.
  Uses existing IDF weights from `feature_dictionary_enhanced.json` (229 keywords).
  Pure Go implementation, no external dependency. ~1.2MB memory for 694 training vectors.
  **Trigger:** keyword miss rate > 10% or accuracy on misses < 70%.
  Reference: `data/classification/tfidf-retrieval.md`
- [x] **5.R2** Embedding retrieval layer (RAG): third cascade layer for semantic matching.
  Uses Ollama `/api/embeddings` endpoint (local, no external API). Handles synonym/brand
  gaps that lexical methods can't bridge. Needs embedding model benchmarking for Portuguese.
  **Trigger:** semantic gap cases > 5% of corrections after TF-IDF is active.
  Reference: `data/classification/embedding-retrieval.md`
- [ ] **5.R3** Value-range plausibility: use `value_ranges` from feature dict to pre-filter
  or post-validate subcategory candidates. Example: R$5000 expense matching "Diarista"
  (max 220) should be flagged as implausible. Orthogonal to few-shot injection — can be
  a separate scoring modifier or pre-filter on the taxonomy sent to the model.
- [x] **5.R4** Historical workbook extraction: extract labeled expenses from pre-2024
  workbooks to expand training set beyond 694 entries. Benefits all retrieval layers
  equally (more examples = better keyword coverage, TF-IDF vocabulary, embedding space).
  DONE session 35 (one-off Python; 2022–2025 → corpus 694→1788 + per-year expense logs).
- [ ] **5.R5** Correction-weighted example selection: when `classifications.jsonl` has
  enough corrected entries, prioritize them as few-shot examples over confirmed/training
  entries. Simple sort order initially (corrected > training > confirmed). Refine to
  weighted scoring if correction volume warrants it.
- [ ] **5.R6** Regenerate `feature_dictionary_enhanced.json` from the post-5.R4 corpus.
  **Found session 63 during a doc-staleness audit — this is a live behavioral gap, not a
  doc bug.** The dictionary was built from the 694-era corpus and never rebuilt after 5.R4
  expanded it to 1,788. It still covers **68 subcategories against the corpus's 81**, so
  **13 leaves have no keyword entry at all**. Consequences: (a) no keyword's dominant
  subcategory is ever one of those 13, so `signal.TopSubcategory` can never equal them — a
  model prediction of one of the 13 therefore always fails the
  `result.Subcategory == signal.TopSubcategory` check and can **never pass the T-32 agreement
  gate**, regardless of model correctness. (Whether `Matched` is true or false depends on
  whether some *other* keyword in the item matched; either way the gate refuses.) Those leaves
  are structurally condemned to manual review; (b) 5.R4's stated benefit ("more examples = better keyword coverage")
  was only ever half-realized — the corpus grew, the retrieval index didn't; (c) IDF and
  specificity scores are computed over a 694-row denominator while the pool is 1,788, so
  every `top_score` the gate reads is drawn from a stale distribution.
  **Before prioritizing:** measure how many of the 649 reviewed rows land on those 13 leaves
  (and how many rows in the keyword-miss stratum 5.R2 handles would become keyword-hits after
  a rebuild) — that quantifies the coverage/precision upside and tells you whether this
  overlaps or competes with 5.R2's territory.
  **Note:** the generator is desktop-era Python; regeneration must preserve the schema in
  `[ref:training-data-schema]` and keep the file gitignored (real expense descriptions).
  Re-running the T-32 gate replay afterwards is the validation gate — `top_score` is an input
  to the shipped predicate, so a rebuild changes production auto-insert behavior.

### Deferred Tooling Improvements

- [ ] **T1** Resume context loading: session-context.md not auto-loaded on resume, so TDD/user-pref directives are missed. Options documented in `.claude/ideas/resume-context-loading.md`. Likely solution: `/resume` skill.

- [x] **T2** Extract the acceptance harness to a standalone repo (a second consumer now exists). The `career-search` `roles` CLI (Go, deterministic, **non-LLM**) plans to adopt `test/harness/`, making it the first external consumer and the real test of the domain-agnostic design. That usage surfaced the expense-shaped leaks to fix during extraction: `FixtureConfig` carries domain fields (`Model`/`Threshold`/`AssertionType`/`AccuracyFloor`/`TopN`) → genericize to `{Command, ExtraArgs, Raw map[string]json.RawMessage}`; `CopyFixtureToWorkDir` is shallow → add recursive `CopyTreeToWorkDir` for tree fixtures; `ollama.go` (`RequireOllama`) sits in the core → a generic CLI-test lib must not assume an LLM (move to an optional `extern` package); `comparator.go` (semicolon CSV + `ConfidenceInRange`) and `Context.{WorkbookPath,DataDir,WorkbookDir}` are expense-specific. **Cleanly generic already:** `Context`/`Scenario`/`Run`, fixture copy/discover, `verify/json.go`, `CommandSucceeded`/`Failed`/`OutputContains`, the `TestMain` build-once pattern. **Approach:** lift the de-domained generic subset into its own module (`Context`/`Scenario`/`Run`, fixture plumbing + `CopyTreeToWorkDir`, generic `verify` base incl. `json.go`, `BuildBinary`/`FindModuleRoot`); both repos depend on it; each keeps its domain `actions/`+`verify/`. **Sequence:** extract *after* the tracker suite is built against a de-domained copy, so a real second consumer shakes out the boundary first. Full analysis: `expense-reporter/test/harness-external-usage.md`; consumer-side plan: career-search `.claude/plans/tracker-cli-acceptance-plan.md` (branch `feat/tracker-cli`).

### Deferred Improvements (from AUTO_CATEGORY_README.md Next Steps)

These items are not blocking for Layer 5.1–5.8 but should be revisited after the core classify/auto commands are working:

- [ ] **5.D1** Add missing subcategories: Utilities (água/luz/gás), Insurance, Credit cards, Subscriptions (identified during initial classification run — see `data/classification/AUTO_CATEGORY_README.md` § "Next Steps")
- [ ] **5.D2** Fuzzy string matching: Levenshtein distance for typo handling (e.g., "Shoope" → "Shopee")
- [ ] **5.D3** Vendor database: canonical vendor name list to normalize inconsistent spellings
- [ ] **5.D4** Seasonal/temporal patterns: day-of-month and month-of-year features for recurring bills
- [ ] **5.D5** ML model experiment: train Random Forest or fine-tune a small BERT on labeled data once enough corrections accumulate (see `data/classification/research_insights.md` § 5 for approach)

### Review UI & `review` Command (session 21)

Local-first review surface — supersedes the Lovable cloud plan
(`docs/plans/lovable-suggestion-plan.md`).

- [x] **RUI-1** Implement `expense-reporter review` command — CSV + workbook taxonomy
  baked into a self-contained `review.html`. Full plan: `.claude/plans/review-command.md`
  **Complete (session 23, 2026-05-18):** All phases done. PR #22 open
  (`worktree-feat+review-command` → `master`). 17 unit tests + acceptance test green.
  Smoke: 349 rows, 23 need review against real classified.csv.
- [x] **RUI-1a** Merge PR #22 (`worktree-feat+review-command` → `master`).
- [x] **RUI-2** Build the `review.html` template via claude.ai/design — brief:
  `docs/plans/review-ui-design-brief.md` + fixtures in `docs/plans/review-ui-fixtures/`.
  Done (session 22): template at `expense-reporter/internal/review/template/review.html`;
  dev-preview copy with sample data kept at repo root `review.html`.
- [x] **RUI-3** `apply` command: ingest the UI's `reviewed.json` back into the workbook
  + feedback logs (`feedback.Entry` confirmed/corrected, `expenses_log.jsonl`).
  **Complete (session 24, 2026-05-29):** Phases 0–3 done. Acceptance test green.
  PR #23 (`feat/apply-command` → `master`). Plan: `.claude/plans/apply-command.md`.
- [x] **RUI-3a** Phase 4 smoke: run `apply` against a real `reviewed.json` from a prior
  review session. Exercises `insertNewRows` (workbook insertion path), which has zero
  acceptance test coverage — the fixture only covers already-inserted and pending entries.
  Index-aliasing bug and dry-run leak already fixed (see commit after Phase 3).
- [x] **RUI-4** Emit the full 3-level path (sheet,category,subcategory) into the
  classified CSV. When done, `review`'s `ReadQueue` populates `Predicted.Sheet` and the
  UI pre-fill becomes unambiguous (the `Predicted` struct already has the optional field).

### Key Decisions (from LLM repo session 32 design)
- Classification logic in **expense-reporter** (Go) — product feature, not LLM infrastructure
- MCP wrapper in **LLM repo** — thin layer, calls the Go binary as subprocess
- Training data strategy: hybrid (feature dict + correction rules as system + top-K few-shot per request)
- Structured output via Ollama `format` param — already proven reliable in LLM infra work
- Model to benchmark: Qwen3-8B (`my-classifier-q3`) vs Qwen2.5-Coder-7B (speed)
- Confidence threshold: HIGH ≥ 0.85 (auto-insert), LOW < 0.85 (present candidates)

<!-- ref:deferred -->
## Deferred / Backlog

- [x] (RUI-WM1) **Workbook mapping Layer 1** — Rewrite `cmd/workbook-inspect/main.go` to output full JSON per sheet: all cells, formulas, cell styles (fill color, bold, borders). Schema in `.claude/plans/workbook-mapping-plan.md`.
- [x] (RUI-WM2) **Workbook mapping Layer 2** — Chrome automation screenshot pass via Google Sheets. Use Layer 1 block list to target sections. See plan.
- [x] (RUI-WM3) **Workbook mapping Layer 3** — claude.ai synthesis of Layer 1+2 into `workbook-generator-spec.md`. Must complete before 2026-07-05 (2× usage expires).
- [x] (RUI-WG) **Workbook generator command** — Implement `generate` command that produces workbook from `classifications.jsonl` + `expenses_log.jsonl`. Blocked on RUI-WM3 (spec).
- [ ] (T-01) **Merge PR #27** — workbook generator branch; user submits/discards pending review first (drafts addressed in session 29)
- [x] (T-02) **Real-taxonomy export** — one-time: Referência (113 subcats) → `taxonomy.json` per spec §1.1; compose sub-item splits into col-B strings; validate via skeleton generation
  - **Addendum (decided session 30, 2026-06-12):** when implementing this export, split `loader.go` (+ the input-only parts of `taxonomy.go`) into a sibling `internal/taxonomy` package — pure input layer, zero excelize — and land the export writer there. Explicit decision NOT to subpackage styles/sheets (Go flat-package idiom; too coupled). **Prerequisite (found session 31):** `taxonomy.go` mixes domain types with mutable RENDER config (`dataYear`/`headroomRows`/`perGroupPctRows`, set by `Generate()`, read by builders); relocate those vars into `generate` before the split, and decide whether the domain types live in `internal/taxonomy` or stay in `generate` (cycle risk to check).
- [ ] (T-03) **Year-rollover workflow** — generate year N+1 skeleton from taxonomy alone; decide `apply`/`add` fate against generated workbooks
- [x] (T-04) **Full-path entry routing** — route logged entries by full sheet/category/subcategory path instead of bare name; changes the entry contract (`expenses_log.jsonl` carries only a bare `subcategory`), the classifier that emits entries, `scanEntries`, and entry-fed fixtures. Advisor-reviewed. Removes the interim ambiguity guard. See `[ref:taxonomy-identity-key]` §6 / `.claude/plans/taxonomy-identity-key.md`.
- [x] (T-05) **Persist expense type end-to-end (Plan A)** — add `type,omitempty` to feedback.Entry + ExpenseEntry, rename ExpenseSheet→ExpenseType, migrate JSON keys (`sheet`→`type`, `sheets`→`types`) with legacy-read fallback, partial backfill from re-exported reviewed.json. Plan: `.claude/plans/persist-expense-type.md`. Hard prereq for T-04. Advisor-reviewed; not yet implemented.
- [x] (T-06) **Bf real-data verification** — execute `.claude/plans/bf-real-data-verification-runbook.md`: export reviewed.json → backfill-type.py → generate-workbook against real config/taxonomy.json; confirm typed entries route (no `not in taxonomy` warnings on typed lines), ambiguous leaves land in the right sheet. The only end-to-end proof of the Plan A→B chain on real data.
- [ ] (T-08) **Generate full-suite acceptance flake (low priority)** — `TestGenerateWorkbook_Skeleton` fails under full-suite timeout (partial actual-dump → nil panic) but passes in isolation. Confirmed benign timeout artifact, not a Phase-R/Type regression. Investigate `test/expect` (was `test/verify`) parallel-safety / temp-dir isolation only if it recurs when run alone.
- [ ] (T-09) **Retire bare-name routing fallback** — now that the classifier emits type (5.R4-classifier, done), shrink/retire the transitional bare-name `byName` fallback in `internal/taxonomy` `scanEntries` as typed coverage approaches 100%; gate retirement on the stderr type-less fallback count reaching ~0. Advisor-flagged risk: deleting it prematurely drops the auto-inserted majority. **Coupled to T-19:** retiring this fallback leans harder on the model path, which (post-T-13) can no longer decline novel inputs — do NOT retire without resolving the T-19 escape-hatch gap first.
- [x] (T-10) **Disambiguate the `5.R4` id collision** — RESOLVED session 35: `5.R4` stays the historical-extraction task (its original owner in the 5.R1–5.R5 series, now done); the classifier-emits-type work is referred to as **`5.R4-classifier`** (adopted in current-status/reading-guide and T-09). Boards now agree.
- [x] (T-11) **Year adaptation for expenses_log** — `parseDate` requires exactly `DD/MM`, forcing per-year log files (5.R4). Accept `DD/MM/YYYY` + have `generate` use a per-entry year (fallback `--year`) so one multi-year log routes directly and the per-year split retires.
- [x] (T-12) **WS-B slice-2 loose ends** — rewire `test/auto_test.go`'s 2 workbook-gated cases (LOW-confidence/ambiguous path currently has no running coverage; one redundant HIGH-path test sits dark) to drop the workbook gate (mirror slice-1 `feedback_test.go`); rename the now-inaccurate `auto.go:162` `✓ Inserted` message (and update the ambiguous-path test that keys on that string).
- [x] (T-13) **Classifier predicts the full `(type,category,subcategory)` path** (revised, designed session 40 — plan `.claude/plans/t13-classifier-full-path.md`). Replaces the original "single-source resolution" framing. `add`/`auto` resolve category from the feature dict but type from `taxonomy.json`; divergent categories (`Fixas – Impostos`, `Fixas – Saúde`) silently emit type-less lines, and 5 multi-type leaves (`Estacionamento`/`Dentista`/pets) are unresolvable by `(category,subcategory)` lookup. **Fix:** classifier predicts the full path against `taxonomy.json` via an atomic 112-path enum (smoke-tested 100% valid, ~6s); `auto`/`batch-auto` become type-less-impossible by construction; `add` (no model) walks `taxonomy.json` + `--type` hybrid for ambiguous leaves. **WS-B resolution-correctness sub-slice** — retrofits done WS-B slices 1–2 and is the precondition for WS-D (drives live-command type-less count to ~0). All 4 design decisions locked (plan §5); `IRFF→IRRF` taxonomy fix already applied; coverage now 100%.
- [x] (T-14) **T-13 live-model verification + model ACCURACY benchmark (gates WS-D)**. **RE-SCOPED session 42:** the Go path is now partially proven live — `classify "Uber Centro" 35,50 15/04 --model my-classifier-q3` ran end-to-end on the real 112-path `config/taxonomy.json` and returned `Uber/Taxi→Transporte` at 100% (no 4xx/5xx). **Key correction:** enum *validity* is **grammar-enforced** (Ollama compiles the `format` enum → GBNF; verified a 9B model is *forced* into valid 112-enum paths, and even forced an out-of-domain item into the enum), so "does the model honor the enum" is NOT a model-capability question — the session-40 qcoder smoke test measured the grammar, not the model. The remaining open question is **accuracy** (does it pick the *right* path), which was never measured across models. **Do:** (a) run the live path on an ambiguous leaf (Dentista/Estacionamento) and confirm a context-right NON-EMPTY type (WS-D precondition — real-data type-less count ~0); (b) **benchmark accuracy** q3 vs q35 vs qcoder on labeled data and set the default to the smallest model that's accurate enough AND fits VRAM (default is now `q3` per session 42; `q35` (6.6 GB) fits too, `qcoder` (20.7 GB) does NOT fit the 12 GB GPU → CPU-offload + load 500s). Needs `data/classification/` populated.
- [~] (T-15) **Surface the predicted `type` in JSON output** (PR #36 / T-13 code-review finding 1). **Slice 1 DONE (session 42):** `Type` added to `CandidateOutput`+`toCandidates` and `AddOutput`+`runAddDryRun` (`type,omitempty`); `verify.OutputJSONHasType` + `thenJSONTypeIs`; unit `TestToCandidates_SurfacesPredictedType` + acceptance `TestAddDryRunJSON_SurfacesResolvedType` green. **Slice 2 (the `--predicted-type` flag) still DEFERRED** (see below). T-13 made the model predict a full `Type/Category/Subcategory` path, but two output structs **discard a type the code already resolved**: `toCandidates` drops `Result.Type` (so `classify`/`auto --json` `CandidateOutput` omit it), and `runAdd` resolves `typ` (add.go:87) then never passes it to `runAddDryRun` (add.go:93), so `AddOutput` omits it too. Direct-insert paths (`auto`/`batch-auto`) already write type to the logs correctly — the gap is only the JSON boundary the MCP layer reads. For 99/104 leaves `add` re-derives type from the unique leaf owner so it's invisible; the cost lands on the **5 cross-type ambiguous leaves** (`Estacionamento`/`Dentista`/`Orion`/`<person E>`/`Ambos`), where the model's prediction-time disambiguation is thrown away before the MCP layer can route it.
  **SCOPED SPLIT (decided in review):**
  - **Slice 1 — DO (cheap, additive, no new semantics): surface the already-computed value.** Add `Type string \`json:"type,omitempty"\`` to `CandidateOutput` + populate in `toCandidates`; add it to `AddOutput` + thread `typ` from `runAdd` into `runAddDryRun`. No resolver change — `resolveFullPath` is untouched and already tested. **Tests worth doing — two, both deterministic, no Ollama:** (1) **unit** (`cmd/.../output_test.go`) `TestToCandidates_SurfacesPredictedType` — marshal `ClassifyOutput{Candidates: toCandidates([]Result{{Type:"Variáveis",...}})}`, assert the JSON string contains `"type":"Variáveis"` (string-level so it FAILS rather than fails-to-compile pre-fix). (2) **acceptance** (`test/json_output_test.go`) `TestAddDryRunJSON_SurfacesResolvedType` — a near-clone of the existing `TestAddDryRunJSON_ResolvesCategory` (same deterministic `Uber Centro;…;Uber/Taxi` input → unique `Variáveis/Transporte`), reusing its exact `Given` (`taxonomyAuthoredWithTrainingData()`); `Then: slices.Concat(thenJSONSucceeded(), thenJSONTypeIs("Variáveis"))` with a new `thenJSONTypeIs` helper wrapping `verify.OutputJSONHasType` (mirrors `thenJSONCategoryIs`/`verify.OutputJSONHasCategory` — result-named, no raw `verify.*` in `Then`, function name mirrors its sibling per `test/PATTERNS.md` + `test/.memories/KNOWLEDGE.md`). Both RED pre-fix (no `type` key), green post-fix; together they cover both output structs. **Do NOT add a separate Ollama-gated "classify candidate has type" test — fold that live proof into T-14.**
  - **Slice 2 — DEFER (speculative API surface), gated on T-14/WS-D:** a `--predicted-type` flag on `add` to record the model's guess *as a prediction* (distinct from a human `--type`), feeding `resolveFullPath`'s `typeFlag` slot, with precedence (human `--type` wins; both subordinate to "unambiguous leaf ignores the flag"). `add` already disambiguates via the existing `--type`, so this is new provenance surface for a round-trip that isn't proven exercised yet — and the manual `add` path may change under the retire-insertion pivot. When built: deterministic acceptance test `add "Dentista …;Dentista" --predicted-type Variáveis` → log `type:"Variáveis"`, twin with `Extras` lands `Extras` (proves it flows), plus keep the non-interactive-no-type → hard-error guard (already current behavior) so the wiring never silently guesses.
  - **PRECONDITION before any new test:** run `./run-acceptance.sh` (`-tags=acceptance`) and confirm the existing JSON-output tests are still green post-T-13 — `add`/`classify`/`auto` now hard-require a configured `config/taxonomy.json` (`runAdd:79` → `TaxonomyFilePath()`), but `json_output_test.go` is NOT in PR #36 and its `classifierForJSON()` set only `DataDir` (that helper is now `taxonomyAuthoredWithTrainingData()` and DOES publish a taxonomy — this precondition describes the pre-T-17 red state, which was since fixed). If those tests are red, fix their `Given` (taxonomy config) first — the new test reuses the same `Given`. Build-tag tests hide from `go test ./...` ([[feedback_rename_json_tag_acceptance]]).
- [x] (T-16) **Model defaults diverged across commands — RESOLVED session 42 by unifying on `my-classifier-q3` (the OPPOSITE of the original fix direction).** Original finding: `classify` defaulted to `q3` while `auto`/`batch-auto` defaulted to `qcoder`, and the proposed fix was to move classify *to* qcoder. **That was reversed once the premise collapsed:** enum validity is grammar-enforced (not model-dependent — a 9B model honors the 112-enum), so qcoder bought nothing but a 20.7 GB footprint that doesn't fit the 12 GB GPU (CPU-offload + load-time 500s). **Fix shipped:** all four defaults (`classify`/`auto`/`batch-auto` flags + `classifier.Config` fallback) set to `my-classifier-q3`; q3 validated end-to-end on the real 112-path taxonomy. **Still TODO (small):** a unit/command-level flag-default-parity test so the three commands' `--model` defaults can't silently drift apart again (deterministic, no Ollama). **Doc cleanup DONE (session 43):** CLAUDE.md classification tier list rewritten — `my-classifier-q3` now primary, qcoder demoted with the grammar-enforcement rationale inline, "required for 5.7+" claim removed. **Still TODO (small):** the flag-default-parity test noted above.
- [x] (T-17) **DONE session 42 — all 11 fixed + verified green** (each got a `fixture-taxonomy.json` covering its input items + `withFeedbackAndTaxonomyConfig`/`SetupBinaryConfig` wired into its `Given`; type-routing reuses its existing `taxonomy.json`; the 2 few-shot classify tests gained a `requireDataDir` skip-guard — note `data/classification` DOES exist at the repo root, so they run for real). `ClassificationAccuracy` passes (q3 ≥50%). **Caveat:** q3 is slow (~12 s/classify → a 10-row batch ≈ 2 min), so the full acceptance package needs `-timeout` raised above the 600 s default, or these run in sub-groups (feeds the T-14 speed benchmark). **PR #36 acceptance regression: taxonomy config now mandatory but many `Given`s don't provide it** (found session 42 running the full `-tags=acceptance` suite — these hide from `go test ./...`). T-13 made `config/taxonomy.json` a hard requirement for `classify`/`auto`/`batch-auto`/`add` (`loadTaxonomyTree` → `TaxonomyFilePath()`), but several acceptance `Given` helpers use `CopyFixtureToWorkDir`/`withFeedbackConfig` and never `SetupBinaryConfig` a `taxonomy_path` → every run errors `taxonomy path not configured`. **Already fixed (session 42, committed):** `json_output` group (3), `add` feedback group (4 — fixture taxonomies + 1 expected category), `TestBatchAuto_MissingWorkbook_FailsFastBeforeClassification` (1, fails before classification → deterministic). **STILL RED (11):** `TestBatchAuto_{Basic,MixedConfidence,ExcludedCategoriesGoToReview,ClassificationAccuracy,OutputDirFlag,InsertFailure_PreservesCSVs,DryRunNoFeedbackLogged}`, `TestFewShot_{ClassifyWithTrainingDataShowsFewShot,ClassifyWithoutTrainingDataSucceeds,BatchAutoProducesOutputFiles}`, `TestTypeRoutingCycle_1_BatchAutoEmitsType`. **CORRECTION (session 42 rerun): these are NOT model-blocked.** Earlier I claimed they couldn't be verified until qcoder loaded — wrong: the batch fixtures pin `model: my-classifier-q3` (passed as `--model` by `actions/commands.go:102` etc.) and the fewshot classify steps use the now-q3 default. q3 fits VRAM and works (`TestAutoJSON` + the live classify run prove it). They fail **purely** on missing `taxonomy_path` in their `Given`, and are **fully fixable + verifiable now**. **Fix per test:** add a `fixture-taxonomy.json` covering the input CSV's expected leaves and configure it in the `Given` (mirror `tenMixedExpensesReadyForBatch` → add `withFeedbackAndTaxonomyConfig`). Independent of T-14.
- [x] (T-18) **DONE session 42 — both `correct` tests green** (seed ids `f0c3bf1293f3`→`8c434a0b64c0` + dates `15/04`→`15/04/2026` in seed+expected; added `fixture-taxonomy.json` with Combustível→Transporte and switched the two `Given`s to `withFeedbackAndTaxonomyConfig`). **Pre-existing (NOT PR #36) `correct` acceptance breakage: orphaned seed ids from DD/MM→DD/MM/YYYY normalization.** `TestCorrect_LogsCorrectedEntryWhenPredictionExists` + `TestCorrect_UsesMostRecentPredictionWhenIdRepeats` fail `no prior classification found`. Root cause (verified session 42): the seeds use `date:"15/04"` + `id:"f0c3bf1293f3"` (= `GenerateID("Uber Centro","15/04",35.5)`), but `parseExpenseForFeedback` normalizes `15/04`→`15/04/2026` so `correct` looks up `GenerateID(...,"15/04/2026",...)` = `8c434a0b64c0` → miss. `parseExpenseForFeedback` is **unchanged by PR #36** → this broke on master when T-11 landed date normalization (build-tag-hidden). **Fix:** in `correct-overrides-confirmed` + `correct-uses-latest-entry`, rewrite seed `seed-classifications.jsonl` + `expected-feedback.jsonl` to `date:"15/04/2026"` and `id:"8c434a0b64c0"` (all lines), and configure a taxonomy (Combustível→Transporte) so the appended corrected entry's `actual_category` resolves to `Transporte` as the expected files assert (today's `withFeedbackConfig` sets no `taxonomy_path`). Fully deterministic (no Ollama) — verifiable immediately, unlike T-17.
- [x] (T-19) **DONE (session 45) — sentinel decline path shipped** (branch `feat/t19-sentinel-decline`). `SentinelPath = "NENHUMA DAS OPÇÕES"` appended to the path enum + system-prompt line; `splitResults` maps it to the `Diversos` leaf (via `taxonomy.ResolveLeaf`) at a FIXED 0.30 confidence (model confidence on a decline is discarded — it scores the forced pick, not pre-constraint uncertainty); dropped if the taxonomy has no Diversos leaf. 4 unit tests (`sentinel_test.go`), TDD. **Live 8-item out-of-domain probe validated the risk AND the fix:** pre-fix, 2/8 wrong picks landed at exactly 0.85 (would auto-insert); post-fix both route to the sentinel → Diversos@0.30 → caught by `auto_insert_excluded`. **RESIDUAL RISK (→ T-14):** the model only declines when *nothing* looks plausible — superficially-plausible wrong leaves still come back at 0.95 (horseshoe→Ferramentas, drone→Lazer/Diamba, skydiving→Saúde/Médico), and q3's self-reported confidence shows large run-to-run variance. Confident-wrong picks are a model-accuracy/calibration problem, benchmark in T-14 before leaning harder on the 0.85 threshold (and before WS-D/T-09). Original finding preserved below.
  Original: the atomic 112-path enum removed the classifier's "none of these" escape hatch (design question raised session 42). Because Ollama grammar-constrains output to the 112 enum members, the model **cannot decline** — every expense, however novel/out-of-domain, is *forced* into some taxonomy leaf. Demonstrated: an "airplane ticket to Tokyo" probe against a 2-member enum was forced to `Aluguel`, and an out-of-domain item came back at high confidence — so the 0.85 auto-insert threshold may NOT catch genuinely-unknown expenses (the model can be forced to emit a confident-but-wrong forced path). The **pre-T-13 algorithm had an explicit escape** (`Diversos`, confidence `0.30`, `require_manual_review`) for no-match items; the enum design has no structural equivalent. Also: `splitResults`'s off-enum drop is now near-dead code (grammar rarely lets an off-enum through). **Decide:** add a sentinel path (e.g. a real `…/Diversos` leaf or an explicit `UNKNOWN` enum member) so the model *can* decline and route low-confidence/novel items to manual review, restoring the safety net. Validate the "forced confident misclassification" risk on real data first (it's one probe so far). Couples to the `auto_insert_excluded: ["Diversos"]` config already in `config.json`.
- [x] (T-20) **Expense-log dedup / idempotency** — `feedback.AppendExpense` is a plain `O_APPEND` write with no dedup on the hash ID, so a `batch-auto` re-run after a partial failure double-appends every previously-succeeded row. batch-auto is the first *batch* user of the append path (add/auto are single-row), so this is now reachable. Add dedup-on-ID (skip if the id already exists in the log) or `--resume` semantics before batch-auto runs at volume on real data. Couples to the slice-3 failure-downgrade (a surfaced partial failure invites the re-run that duplicates).
- [x] (T-22) **Taxonomy descriptions for the classifier prompt** (idea, session 45 — couples to T-14, do NOT build before benchmarking). Type names (`Fixas`/`Variáveis`/`Adicionais`/`Extras`) encode user semantics a small model has to guess; the T-19 probe showed it dumping into the vaguest-*named* leaf. Add an optional `description` (JSON `omitempty`) to `taxonomy.ExpenseType` (type-level only first — 3–4 lines, nearly free; leaf-level ×112 needs prompt-size/latency measurement), rendered only by `buildSystemPrompt`/`writeTaxonomyTree` — no enum/grammar/routing change, backward-compatible. **Open design question:** authoring home — `taxonomy.json` is exported from the workbook (T-02), so descriptions live in the export flow or a sidecar, else regeneration wipes them. **Validate via T-14:** benchmark labeled accuracy with vs. without descriptions before adopting.
- [x] (T-21) **Reviewed-installment under-recording** — `apply` records a reviewed installment expense (e.g. `99,90/3`) as a SINGLE per-installment row: the installment count is discarded upstream at `review.ReadQueue` (`queue.go:63`, `_`) and absent from the `reviewed.json` contract, so `apply` cannot expand it (count=1). batch-auto's AUTO path expands correctly; the review→apply path does not. Fix threads the count/rawValue through `review.ReadQueue` → the HTML export JS → the `reviewed.json` schema → `apply.ReviewedEntry`. UX design question: show 3 rows or 1 row with a ×3 badge. Orthogonal to the log-append pivot; surfaced during WS-B slice 4.
- [ ] (T-23) **Calibration / auto-insert gate rethink (blocks WS-D)** — T-14 measured that q3's confidence is uninformative: ~91% of WRONG full-path answers carry confidence ≥0.85 (means 0.95 correct vs 0.90 wrong, both think modes), so the 0.85 auto-insert threshold filters almost nothing. Before WS-D can lean on the model path, replace or repair the gate: candidate directions — margin/agreement-based thresholding instead of raw self-reported confidence, few-shot pairs with varied confidence (the synthetic 0.95 pairs likely anchor the model), review-first default for non-recurring items (leaky-item accuracy 71.7% vs clean 49.1% — recurrence is a better safety signal than confidence), stronger `auto_insert_excluded` rules. Report: `.claude/t14-benchmark-report.md`.
- [x] (T-24) **Decide the `--think` default (after T-22)** — T-14 shipped a `--think` flag (default true). No-think q3 is 10× faster (1.5 s vs 14.4 s/item) at −3.3 pp full-path (59.7% vs 63.0%) but drops the T-19 sentinel to 0/20 on out-of-domain probes. If T-23 moves auto-insert safety off confidence anyway, the speed case for defaulting batch-auto to `--think=false` strengthens. Decide per-command defaults after the T-22 A/B (which should run both think modes if it measures the sentinel).
- [ ] (T-25) **q35 grammar-drop bug upstream + persona cleanup** — verified on Ollama 0.17.5: qwen3.5 + `think:false` + `format` silently drops the grammar (free-form JSON, out-of-enum labels); `/no_think` soft switch also ignored by qwen3.5; with thinking on, q35 runs 70–240 s/item (unbounded thinking). Reproduce a minimal case, check newer Ollama versions, file upstream if still present. (The inert `my-classifier-q35-nothink` persona was already deleted by the user, session 46.)
- [x] (T-26) **T-22 think-on confirmation run** — run the deferred think-on A/B for type descriptions: confirm the +6 pp type gain holds in the production-default (think-on) mode AND measure OOD sentinel-decline rate WITH descriptions (only alive in think-on). Must toggle the tracked sidecar (`config/type-descriptions.json` present=descen, absent=nodesc), NOT swap `taxonomy.json` — `run_t22.sh`'s current mechanism is stale (warning in its header). ~72 min. Couples T-23/T-24. Report: `.claude/t14-benchmark-report.md` "T-22 Addendum".
- [ ] (T-27) **Leaf-level taxonomy descriptions** — extend T-22 (type-level) to per-leaf descriptions (×112) to attack the "right sheet, wrong leaf" errors type-level text leaves untouched. Needs prompt-size/latency measurement first (112 blurbs vs 4); would extend the sidecar schema beyond the flat type→desc map. Validate via the T-14 harness before adopting.
- [ ] (T-28) **Alternative classification strategies** — explore beyond the single-shot atomic-enum path prediction: (a) two-stage — classify type (and/or category) first, then derive the subcategory within that branch; (b) two-pass grammar — classify UNCONSTRAINED (free-form) first, then re-ask WITH the GBNF grammar only if the free-form answer doesn't match the taxonomy tree. Measure accuracy/latency vs the current one-shot baseline on the T-14 sample.
- [ ] (T-29) **Larger OOD probe set for sentinel significance** — the T-19 sentinel-decline rate is measured on only 20 synthetic out-of-domain items (`.claude/scratch/t14-benchmark/ood.jsonl`). T-26's think-on run saw 4/20 (nodesc) vs 0/20 (descen), Fisher exact p=0.11 — **underpowered**, so "descriptions suppress the sentinel" could not be confirmed or refuted. Expand the OOD set (~60–100 items) so decline rate, description effect, and think-mode effect clear significance. Feeds T-24 (`--think` default) and validates the T-19 safety net. Reuse `run_t26.sh` + `sigtest_t26.py` (Fisher already wired). Report → `.claude/t14-benchmark-report.md`.
- [x] (T-30) **Leaf-first classification (accuracy A/B + T-23 precondition)** — make the classifier predict the LEAF (enum = 104 leaf names) and derive type/category upward via `taxonomy.ResolveLeaf`, replacing the type-first full-path enum. Attacks T-14's dominant "right sheet, wrong leaf" error class; mirrors the manual `add` path. **D1 LOCKED (session 49) = second `type` field, schema order leaf-THEN-type, always-predict, ResolveLeaf disambiguates the 5 collisions + cross-checks the 99 as a free calibration signal.** Open: D2 (tree vs flat prompt), D3 (benchmark-only vs prod — lean benchmark-only), D4 (adopt threshold). A/B vs current enum on the T-14 sample (full-path/type/leaf accuracy + wrong-leaf rate; stratify leaky/clean). Precondition for T-23 (makes any token-confidence signal legible). Negative result is valid. Plan: `.claude/plans/leaf-first-classification.md`.
- [x] (T-31) **5.R2 NN-retrieval precondition (gates the embeddings build)** — the 649-replay ruled out 5.R1 and points at 5.R2, but softened: "160/160 misses have a same-subcat pool-mate" ≠ those pool-mates are embedding-retrievable. Before building 5.R2, embed the 160 no-keyword-match miss items (`.claude/scratch/replay-649/retrieval.jsonl`, `n_examples==0`) + their pool-mates via Ollama `/api/embeddings`, measure nearest-neighbor subcat hit-rate. Weak → novel items are permanent-review, not 5.R2. The settled next step. See `.claude/scratch/replay-649/FINDINGS-model.md`.
- [x] (T-32) **Wire the specificity/agreement gate into `IsAutoInsertable` (WS-D unblock)** — replace the confidence-only gate (dead per session-52 replay) with the specificity `top_score` + model⊕keyword-agreement signal (spec=1.0 ∧ agree → 95% subcat). Expose match-strength (+ keyword-top1) from `SelectExamples` → thread out of `Classify` (return-shape change) → widen `IsAutoInsertable` → 3 call sites (`auto.go`×2, `batch_auto.go`). No new inference; gate runs no-think. HOLD: design gate-to-review, not silent insert (sample can't prove silent-insert precision).
- [ ] (T-33) **Add a `ref:quick-pointers` block to `.claude/session-context.md`.** Session 111 migrated `resume.sh` to a config-driven step list (`.claude/resume.yaml`, session-tracking overlay v11). Its "Key files" step resolves the `quick-pointers` role, now present in `.claude/handoff/registry.yaml` pointing at `.claude/session-context.md`. **This repo has no such block**, so the step prints its fallback `(no ref:quick-pointers block found)` — exactly what the old bash printed, so nothing regressed. But the section is worth having: it is the 6-row table telling an agent where `tasks.md`, `session-log.md`, `session-context.md`, `CLAUDE.md`, and the cross-session memory live, injected at every session start. Wrap it in `<!-- ref:quick-pointers -->` / `<!-- /ref:quick-pointers -->`; the step then picks it up with no config change. Alternatively, delete the step from `.claude/resume.yaml` and the `quick-pointers` role from the register. See llm repo `docs/plans/resume-config-steps.md`.
- [ ] (T-33) **Ref-integrity repair** — `check-ref-integrity.py` reports 4 pre-existing errors: dangling `[ref:bash-wrappers]` (cited by CLAUDE.md Documentation Rules but no block defines it — wrap the index.md Tools table) and duplicate block definitions for `classification-overview` / `confidence-thresholds` / `training-data-schema` (each defined in both `.claude/index.md` and a `data/classification/*.md` file → `ref-lookup.sh` resolution ambiguous; keep one canonical home each). Plus 6 orphan-block warnings (defined, never tagged) to triage. Found session 54.
- [x] (T-34) **auto_log_append_test.go sends gate-failing `Uber Centro` as a CLI arg** — T-32 (s57) swapped `Uber Centro`→`Posto Ipiranga` in auto-append *fixtures*, but this file passes the item as a literal `When:` arg (`RunAuto("Uber Centro", …)`, ~lines 30 & 60), so 2 tests (`TestAuto_HighConfidenceAppendsToLog`, `TestAuto_HighConfidenceInstallmentsExpandToNEntries`) still send it → keyword spec 0.8 + ambiguous (Viagens/Uber-Taxi) → correctly rejected by the agreement gate → never appends → `classifications.jsonl` never created → RED. Deterministic (model-independent). **Fix:** swap both `When:` items to `Posto Ipiranga` and update the stale file comments claiming `Uber Centro`→100%. Surfaced by the Session-B Phase-0 baseline (`.claude/scratch/phase0-roster.txt`); NOT caused by the migration (roster identical pre/post).
- [x] (T-35) **auto's raw-vs-normalized date diverges the join id across the two logs** — `runAuto` passes the RAW date string to `logConfirmedFeedback` but the NORMALIZED `time.Time` to `appender.ExpandAndAppend`, so input `15/04` logs `date:"15/04"` in `classifications.jsonl` and `date:"15/04/2026"` in `expenses_log.jsonl` → `GenerateID` (sha256 of item+date+value) differs between the two files for the SAME expense, breaking the cross-file join key. Same root-cause family as T-18. Fix: normalize once in `runAuto` (format the parsed date back to `DD/MM/YYYY`) before BOTH consumers; check `add`/`batch-auto` for the same pattern. Found session 60 while repairing T-12.
- [x] (T-36) **Cosmetic/staleness sweep (batch in one small PR)** — (a) rename `TestAuto_HighConfidence{AppendsToLog,InstallmentsExpandToNEntries}` — names describe the pre-T-32 confidence gate; the agreement gate decides now; (b) `test/fixtures/auto-basic/input.csv` header references a `TestAuto_Basic` that doesn't exist (and the file is unused by the current tests) — fix or delete; (c) README batch-auto "Output files" still lists `rollover.csv` — retired in WS-B slice 3; (d) `gofmt -l` flags 5 pre-existing `internal/batch/` files (CRLF-era, same class session 59 cleaned in `test/`) — run gofmt + confirm `.gitattributes` coverage. Found session 60.

- [ ] (T-37) **`config.json` `date_year` is dead config** — `DateYear` is declared at `internal/config/config.go:13` and read by **nothing**: it has exactly ONE reference in the entire codebase (the struct field itself). Meanwhile `expense-reporter/.memories/KNOWLEDGE.md` "Config Design" states `date_year` is "applied to DD/MM dates during parsing" and that year rollover "requires updating `date_year` in config" — an instruction that has never had any effect. **Decide:** wire it as the fallback year (replacing the implicit `time.Now().Year()` inside `ParseDateFlexible` for `auto`/`batch-auto`/`add`/`correct`, which is what the doc claims already happens), or delete the field and correct the doc. Wiring it would also close the "December batch entered in January lands in the wrong year" hole that `batch-auto --resume`'s flag help currently just warns about (T-20). Found session 61 during the T-35 date survey; detail in `.claude/t35-date-year-semantics.md` §5.
- [x] (T-38) **`utils.ParseDate` hardcodes year 2025** — `pkg/utils/date.go:41` builds every date as `time.Date(2025, ...)` regardless of the actual year, and it is **live**, not dead: `internal/batch/processor.go` → `parser.ParseExpenseString` → `models.NewExpense` → `ParseDate`. So every bare `DD/MM` date entered through the plain `batch` command (the manual, non-classifier path that still writes the workbook) is silently stamped 2025. Deliberately NOT bundled into T-35 because it changes workbook data rather than log metadata. Likely resolved together with T-37 (a configured fallback year would replace the constant); check whether `ParseDate` has any remaining caller once `batch`'s fate is settled under WS-E. Found session 61.
- [x] (T-39) **Acceptance suite now exceeds `run-acceptance.sh`'s own timeout** — a full run measured **1989s**, but the script hardcodes `-timeout 1800s` (`run-acceptance.sh:49,51`), so **the sanctioned way to run the suite would now time out**; session 61's green came from invoking `go test` directly with `-timeout 2400s`. Runtime is also highly variable — the same suite ran **840s** on one branch and 1989s on another, same machine, ~2.4× spread (session 60 measured 1311s), consistent with Ollama model-load/contention rather than the tests. **Decide:** raise the script's ceiling (cheap, kicks the can), or split the Ollama-gated tests into their own group so the deterministic suite stays fast and the slow group is run deliberately (more work, stops the ratchet). Couples to T-24 (`--think=false` would cut per-classify latency ~10×). Distinct from T-08, which is a single-test timeout artifact rather than total runtime vs the ceiling. Found session 61.
- [x] (T-40) **`batch-auto`'s join-id write has no test coverage** — T-35 fixed the raw-vs-normalized date in `logConfirmedFeedbackForRow`, but unlike `auto` and `apply` that path has **no dedicated test**: every batch-auto fixture uses full `DD/MM/YYYY` dates, which by construction cannot expose this bug class (raw == normalized). **Options:** a short-`DD/MM` batch-auto fixture asserting `expect.JoinIDMatchesAcrossLogs` (~2 min of Ollama runtime, but the suite is already over its timeout ceiling — see T-39), or a unit-level seam around `logConfirmedFeedbackForRow` that avoids the model entirely (preferred). Note any such fixture MUST keep the short date: a full one silently disables the assertion. Found session 61.
- [x] (T-41) **Parse boundary — structured input, parse-once** — one string→struct conversion (`ParsedExpense`: item, canonical `DD/MM/YYYY` date, value, installments, rawValue); year precedence `string > --year > config date_year > most-recent-non-future` (grace window TBD); absorbs T-37 (wires `date_year`) + T-40 (join-id unit seam becomes a boundary test); T-21 threading is the first consumer slice (UX locked: 1 row + ×N badge); `--year` flags added uniformly to `auto`/`batch-auto`/`add`. T-38 explicitly OUT (dies with plain `batch` under WS-E). Design session opens (plan §10): package home/struct shape, migration order (candidate: add/correct → auto → batch-auto → apply), currency-normalization ownership, MCP `parse_expense` timing. Plan: `.claude/plans/parse-boundary.md`.
- [x] (T-42) **First real monthly close on 2026 data (organizing milestone)** — run an actual bank CSV through `batch-auto → review → apply → generate-workbook --year 2026`; everything that breaks or annoys becomes the real backlog. Session-62 reframe: the project is PRE-FIRST-USE — a dogfood run converts the backlog into the few things that actually block usage. Pre-work order: T-41 → T-21 → T-03 rollover check → promote all-years log (user decision). Context: `docs/expense-classifier-vision.md`, `.claude/t23-strategic-implications.md`, parse-boundary plan §9.
- [x] (T-43) **Explicit-year the `correct` tests' bare-date inputs** — `TestCorrect_LogsCorrectedEntryWhenPredictionExists` + `TestCorrect_UsesMostRecentPredictionWhenIdRepeats` send bare `15/04` in their `When:` strings against seeds dated `15/04/2026`; they pass today (rung 4 resolves a past date to the current year) but go RED in January 2027 (bare `15/04` → 2027 → `GenerateID` lookup miss → "no prior classification found"). Fix: the two `When:` strings → `15/04/2026`. Safe — these assert the lookup JOIN, not raw-vs-normalized divergence, so they are NOT the T-35 short-date class (which must stay short). Found session 63 during the T-41 slice-1 audit; contrast fixture `correct-year-flag` which pins the prior-year join via `--year 2024` deliberately.
- [x] (T-44) **Stale test-helper name in tasks.md** — line ~190 cites `classifierForJSON()` in a TC precondition note; the helper was renamed to `taxonomyAuthoredWithTrainingData()` in session 64 (and later de-parameterized). One-line fix; could not be done inline because the handoff pipeline rejects edits outside registered regions.
- [x] (T-45) **Branch cleanup after the PR split lands** — delete `backup/t41-full-20260723` (holds all 16 pre-split commits) once PRs #54 and #55 are merged and the split is confirmed good.
- [x] (T-46) **`DateString()` could emit a year it cannot render as `DD/MM/YYYY`** — DONE session 65 (PR #56). `DateString()` is the SOLE producer of the bytes hashed into the join id shared by `classifications.jsonl` and `expenses_log.jsonl`, so a year outside 1..9999 put a malformed field (`15/04/-005`, `15/04/12345`) into that key. The filed report blamed `Options.Year`; probing showed **all three** year-supplying rungs unvalidated (year typed into the date string, `--year`, config `date_year`). Fixed with ONE check on the RESOLVED year rather than three per-rung checks — `validateRenderableYear` in `internal/parse`. Coverage the future-year rule would mask is preserved by a direct unit test (`TestValidateRenderableYear_Boundaries`).
- [x] (T-47) **Refuse entering an expense dated beyond the current year** — DONE session 65 (PR #56), user policy call, PROVISIONAL. Year-scale on purpose, not day-scale: a date later in the current year is accepted, because the failure worth blocking is a mistyped YEAR, which is silent and expensive (the row hashes and writes cleanly to both logs, then `generate-workbook --year N` never routes it and it vanishes with no error). Guards only what a human types — installment expansion builds later dates downstream from an already-parsed `time.Time` and never re-enters the boundary, so a 24× purchase still writes rows years ahead, deliberately. Kept as a separate function from `validateRenderableYear` so the policy can be relaxed on its own.
- [ ] (T-48) **Past-side year bound is unguarded** — `15/04/1900` and `--year 1` are accepted: renderable, and not future. Same wrong-year harm T-47 blocks in the other direction — a typo of `2016` for `2026` writes cleanly, then vanishes from the workbook. **Deferred by user decision (session 65)**; referred to as "T-47b" in that session's conversation. Genuinely harder than the future side: there is no natural floor, and the 5.R4 corpus legitimately spans 2022–2025, so any bound risks blocking real backfill. The stale-`date_year` warning shipped in PR #57 is the targeted version of this guard for the case that actually bites. Revisit only if a real mistyped-past-year incident occurs.
- [x] (T-49) **Decide the stale-`date_year` warning's shape for `batch-auto` (T-41 slice 3)** — found by advisor review, session 65. `warnIfStaleConfiguredYear` is called once per parse. `add`/`correct` are single-row, so that is one line per run; when slice 3 repoints `batch-auto` to the boundary, the natural per-row placement emits one identical warning per bare-dated CSV row — 300 lines for a 300-row statement, which buries the batch's own output and trains the user to ignore stderr. Decide the batch shape while writing slice 3 (hoist above the row loop, or gate on a first-fire flag), not during the T-42 dogfood run.
- [ ] (T-50) **`--year 0` is indistinguishable from unset in the year ladder** — found by advisor review, session 65; PRE-EXISTING from T-41 slice 1, not introduced by the date_year work. `resolveDate` tests `opts.Year != 0`, so `0` is the "unset" sentinel and an explicit `--year 0` silently falls through to the config rung. New wrinkle now that the stale-`date_year` warning exists: such a user gets the configured year AND a warning advising them to "pass `--year` to override" — which they just did, so the message reads as wrong. Low priority (`--year 0` is not a meaningful year, and `validateRenderableYear` would reject it if it reached the date). Options: reject `--year 0` explicitly at the flag, or use `*int`/a set-flag to distinguish unset from zero. Note `validateRenderableYear`'s doc comment records that year 0 is unreachable for this exact reason — any fix must update it.
- [ ] (T-51) **Decide `auto --json`'s shape under the T-47 future-year refusal** — after T-41 slice 2, a future-dated input makes `auto --json` hard-error instead of returning a `would_insert`/`review` recommendation, so a path documented as read-only ("never inserts", KNOWLEDGE § JSON Output Mode) became a refusal path. The MCP layer (`mcp-server/` `classify_expense`) reads this JSON, so the decision has a real consumer. Defensible as-is — T-47 is an input-validity rule and a mistyped year is exactly the silent failure it exists to catch, better refused at the probe than recommended and written later. The alternative is an `action: "rejected"` payload naming the future-year problem, preserving "read-only never fails" as a property the wrapper can rely on. **Deferred by user decision (session 66) to the T-42 dogfood run**, which will show whether the refusal is right in practice.
- [ ] (T-52) **`auto`'s argument shape diverges from the vision** — `docs/expense-classifier-vision.md` Phase 1 specifies `auto "item;DD/MM;value"`, the semicolon form `add` and `correct` use and the form the Telegram layer would post (Phase 4 receives exactly that string from chat). Shipped `auto` instead takes three positional args `<item> <value> <DD/MM>`, **transposed** relative to `parse.Fields(item, dateStr, valueStr)` — which is what makes its boundary call site error-prone (a swap compiles and parses the value as a date). Found session 66 during slice 2. Decide whether `auto` gains the semicolon form (matching the vision and its siblings, and letting it reuse a boundary wrapper) or the vision is amended to the 3-arg shape. Couples to T-51 since both are `auto`-interface questions for the T-42 dogfood.
- [x] (T-53) **`run-acceptance.sh` can report a cached pass that never ran the change** — the suite builds the CLI inside `TestMain` (`harness.UseBinary`/`BuildBinary`), so Go's test cache keys on the *test package's* inputs and edits under `cmd/`or `internal/` do not invalidate it. The script passes no `-count=1` and forwards only `-keep-on-failure`/`-keep-artifacts` (`run-acceptance.sh:53-73`), so there is no way to force a re-run through the sanctioned entry point. Hit live in session 66: the first post-change run printed `ok expense-reporter/test (cached)` and had executed none of the new code; the real evidence needed `go test -tags=acceptance -count=1 ./test/...` invoked directly. **Fix:** add `-count=1` to the script's `go test` invocations (cheap, and correct for an integration suite whose subject is an external binary), or whitelist it into `EXTRA_FLAGS`. Same defect family as `commandFailed()`-only assertions and full dates hiding join-id bugs: the mechanism reports success while testing nothing.

- [x] (T-54) **`review` cannot read `batch-auto`'s output — `auto_inserted` format mismatch** — `writeClassifiedCSV`/`writeReviewCSV` emit `true`/`false` in the `auto_inserted` column, but `review.ReadQueue` (`internal/review/queue.go:73-81`) accepts only `"1"`/`"0"` and returns a hard error on anything else. So `expense-reporter review classified.csv` fails outright on real batch-auto output; the acceptance suite hides this because `type-routing-cycle`'s `review-input.csv` fixture carries the true/false→1/0 conversion **by hand** (documented as a deliberate fold point in `test/.memories/KNOWLEDGE.md`). This breaks the middle of the T-42 chain (`batch-auto → review → apply`). **Decide which side moves:** emit `1`/`0` from the writers (cheapest, but the column is also read by humans), or accept both spellings in `ReadQueue` (more forgiving, and the reader already owns the parsing). **Do it with T-21** — that also touches `ReadQueue`, and the reader should be changed once. Found session 67 while tracing the canonical-date change through to `apply`.
- [ ] (T-55) **`apply`'s `--year` default makes the config rung unreachable** — `applyYear` defaults to `time.Now().Year()` (`apply.go:41`) while `add`/`correct`/`auto`/`batch-auto` all default to `0`, the "unset" sentinel. Since T-41 slice 4 routes `apply` through the year ladder, a non-zero `Year` means rung 2 ALWAYS fires, so config `date_year` (rung 3) and most-recent-non-future (rung 4) can never apply to `apply`. It also builds `parse.Options{Year: year}` directly instead of going through the shared `parseOptions(yearFlag, appCfg)` helper, so `ConfigYear` is never passed at all — the one command that skips the helper, which is exactly the drift `parseOptions` exists to prevent. **Fix:** default the flag to `0` and route through `parseOptions`, so `apply` obeys the same ladder as everything else. Note this interacts with T-50 (`--year 0` is indistinguishable from unset) — decide them together. Found session 67 during the slice-4 handoff review.
- [ ] (T-56) **Carry unreviewable rows into the review UI** — S1 option (c), deferred by the user in s68. Rows `batch-auto` could not parse are currently skipped and named on stderr only; the human who could actually repair them never sees them in the browser. Needs real UI work: the page assumes every row has a date and value, so an unreviewable row needs its own affordance (show the raw text, let the user retype it, or mark it dismissed). Fold into a T-21-adjacent pass since both touch `ReadQueue` → export JS → `reviewed.json`.
- [ ] (T-57) **Retire `config.json` `workbook_path` (and possibly the `--workbook` global flag)** — it names `../Planilha_BMeFBovespa_Leandro_OrcamentoPessoal-2025.xlsx`, which does NOT exist anywhere on disk (found by the s68 scout). Since S2, nothing on the live path reads a workbook: `excel.LoadReferenceSheet` has only `internal/batch` and `internal/workflow` as callers, both WS-E dead code. Decide with WS-E whether the config key and the global flag go too.
- [ ] (T-58) **`batch-auto --help` is stale** — still says it auto-inserts "into the workbook" and describes `--dry-run` as "skip workbook insertion"; WS-B slice 3 made it append to `expenses_log.jsonl`. T-36 cosmetic family. Check the other commands' Long text in the same pass.
- [x] (T-59) **Commit the T-42 scout as a repeatable pre-close check** — s68 built it ad hoc (scratch install root: binary + its own `config/` + a copy of `data/classification`; run the chain; assert the real logs are byte-identical before/after). That recipe currently lives only in prose in `.claude/t42-scout-report.md`. Promote it to a script so "the chain works on real data" is a check rather than a claim re-derived each session, and so the isolation cannot be got subtly wrong (`--data-dir` does NOT isolate the logs).
- [ ] (T-60) **`apply` can leave a PARTIAL installment series** — created by T-21 (s69). `appender.ExpandAndAppend` appends row-by-row and returns on the first error, while the classification entry is written only AFTER a successful append. A mid-series I/O failure therefore leaves k of N expense rows with NO classification entry — and a re-run finds nothing in `classifications.jsonl`, treats the row as new, and re-appends the whole series, duplicating the k that landed. Impossible before T-21: apply only ever wrote one row, atomic by construction. T-20 deliberately designed `batch-auto` to route partial series to review and never auto-complete them; `apply` is now a second producer of partial series WITHOUT that guard, and it has no ledger at all (`LoadExpenseIDCounts` / `warnDuplicateEntries` are `batch_auto_resume.go`-only), so it cannot even warn. Options weighed in s69: (a) document only [chosen, to keep T-21 scoped], (b) give `apply` the ledger — reuses batch-auto's machinery and makes it per-installment idempotent, (c) make `ExpandAndAppend` all-or-nothing at the shared layer — smallest conceptual fix but changes behavior for all four callers, and true append atomicity is not free. Detail: `internal/apply/.memories/KNOWLEDGE.md` § Known Limitations.
- [ ] (T-61) **The `exportReviewed()` JS hop is UNGUARDED** — measured by mutation in s69, not assumed: deleting `installments: s.entry.installments` from the export leaves the ENTIRE Go suite green, because the export runs in the browser and no Go test can execute it. Blocked on toolchain, not on effort: the `playwright` npm package is not installed (only browser binaries are, from the MCP plugin) and the Playwright MCP is pinned to an absent system Chrome (`/opt/google/chrome/chrome`), so a committed browser test needs either a Node devDependency or `playwright-go` in a Go-only repo — a real architectural decision, deferred by the user in s69. Partial compensation already shipped: `validateEntries` hard-errors on a missing `installments`, so a silently broken export makes `apply` REFUSE the file rather than under-record every installment purchase forever; and the Go half of the chain is pinned by `installmentCountOfferedToTheReviewer` in `test/review_test.go`. **Do NOT close this with a hand-authored fixture** — a `reviewed.json` written to contain the field asserts a contract the producer may not implement, which is exactly the T-54 shape. Detail: `test/.memories/KNOWLEDGE.md` § "The export JS is the one hop no Go test reaches".
- [ ] (T-62) **`resume.sh` silently truncates a ref block** — observed directly in s70: its `active-decisions` section ended at a bare `### Parse boundary — COMPLETE (session 67)` heading with NO body, while `ref-lookup.sh active-decisions` returned four further sections (Test architecture, Close cycle, Cross-boundary counts, Running the close). Nothing marks the cut. **Two harms, the second severe:** (a) a session that reads `resume.sh` for orientation silently gets a partial picture of the locked decisions — exactly the content most likely to be re-litigated; (b) the session-handoff skill authors replace-mode blocks from the CURRENT interior, so authoring `active-decisions` from the resume rendering would have DELETED the missing sections and the verifier could not have caught it (it checks that nothing moves OUTSIDE a registered region, not that a region kept its content). Same "reports success while testing nothing" shape the repo keeps cataloguing, this time in the tooling that reports status. **Find the cap** — likely a line/char limit in `.claude/resume.yaml`'s `region:` step or in `resume.sh` itself — and either raise it or make truncation VISIBLE (print `… N lines truncated, run ref-lookup.sh <key> for the full block`). Check whether `current-status` and `session-reading-guide` are also affected: both LOOKED complete in s70 but neither was verified against `ref-lookup.sh` output, and both are replace-mode roles with the same deletion risk. Interim rule already recorded in the s70 log entry: re-fetch replace-mode interiors with `ref-lookup.sh`, never trust the resume rendering.
- [x] (T-64) **Multiplier installment notation `405,25 x4` — a SECOND installment syntax whose value means the OPPOSITE of the existing one.** Planned by the user long before s71 but never written down; surfaced when the first monthly close rejected the two rows that use it (`expenses-2026-01.csv` lines 19 and 45). **The crux, and the reason this is not a small parser tweak:** `utils.ParseCurrencyWithInstallments` (`pkg/utils/currency.go:43`) treats the written number in `total/N` as the TOTAL and DIVIDES it — `405,25/4` → 4 rows of **101,31**. The `x4` form is Brazilian retail usage ("4x de R$405,25") and means the number is PER-INSTALLMENT, so it must MULTIPLY — `405,25 x4` → 4 rows of **405,25**, total 1.621,00. Same digits, **4× apart in the budget** (101,31/month vs 405,25/month; 405,25 vs 1.621,00 total), and both spellings are plausible to a reader. **CONFIRM THE DIRECTION WITH THE USER BEFORE IMPLEMENTING** — guessing reproduces the T-21 harm (silent under/over-recording that is indistinguishable from a legitimate entry) with a bigger multiplier. **Both token orders appear in real data:** line 19 is `405,25 x4` (x before N), line 45 is `646,25 4x` (N before x); decide whether to accept one, both, or normalize. **Implementation is a single chokepoint with two live consumers:** `ParseCurrencyWithInstallments` is called by the parse boundary (`internal/parse/parse.go:103`) and by `review.ReadQueue` (`internal/review/queue.go:86`) — and by the retired `models.NewExpense` (`internal/models/expense.go:44`, WS-E). **Both live callers must learn it together or the count dies at the review boundary**, which is precisely the T-21 bug (auto route expanded, review route did not). Add a `classified.csv` → `ReadQueue` seam case alongside the existing T-54 one. **DOC TENSION — resolve as part of this task:** `.claude/t42-scout-report.md` § S4 carries an explicit user correction stating that `- 1/4` and `4x` are input ERRORS and that "T-21 needs no new notation work"; `test/fixtures/batch-auto-failed-rows/README.md` repeats it ("Do not add parser support for them"). That ruling is SUPERSEDED for the multiplier form and both files will actively mislead until updated — the fixture is the more dangerous one, since it tells a future reader not to do this. **Line 45 is NOT unblocked by this task alone:** `<person C> compra chocolate Ruby 299,00 e cacau 49,90;646,25 4x` also has no date at all, and its arithmetic does not reconcile (299,00 + 49,90 = 348,90 ≠ 646,25), so it needs a data decision from the user independently of the notation. **DONE (s73, branch `feat/t64-multiplier-notation`).** Mode switch = PRESENCE of `x`/`X`; the value/count split is resolved by trying three readings (`value x count`, `value count x`, `count x value`) and requiring EXACTLY ONE to hold, so `4x5` and `646,254x` are REJECTED as ambiguous rather than guessed. Implemented at the single chokepoint `utils.ParseCurrencyWithInstallments`; both live callers inherit it because `review.ReadQueue` now routes through the new `parse.Value` instead of calling utils directly — which also fixed a live BR-thousands seam bug found the same session (see T-72). The DOC TENSION named above was already resolved in s71 and is now flipped to "landed"; the `batch-auto-failed-rows` fixture needed NO change, because both of its `x`-notation rows are rejected on field count before the value is parsed (verified by running it).
- [ ] (T-65) **An auto-inserted row cannot be corrected — the budget stays wrong.** Full write-up + options: `.claude/t42-close-findings.md` [ref:auto-insert-unrepairable]. One-line version: `correct` and `apply` both write feedback only, both logs are append-only with no supersede, so a category fixed during review never reaches `expenses_log.jsonl` or the workbook. Measured on the first close: `4x álcool 70` is still filed as fuel. Affects only the ~25% of rows auto-appended without review, whose absolute precision is unmeasured. Decide between supersede-by-append (a), in-place rewrite (b), or gate-to-review (c, = WS-D). Recommendation in the report: (c) as the direction, (a) regardless.
- [ ] (T-66) **Classifier accuracy on the first real close: 38% correction rate, 92% of errors cross-CATEGORY, and the keyword layer already held the reviewer's answer 60% of the time.** Full write-up, per-row table, method + caveats: `.claude/t42-close-findings.md` [ref:close-accuracy-2026-01]. Consequences the report argues: **T-27 is aimed at the wrong failure** (it targets wrong-leaf; the measured errors are wrong-branch) — re-justify against these rows or drop it in favour of **T-28**; **T-32 confirmed on real data** (median confidence on WRONG answers 0.95); **5.R6 has a concrete payoff** (`exame`/`exames`/`veterinário`/`nefro` absent from the 694-era dictionary); and the cheapest intervention is to show the KEYWORD's answer in the review queue when it is unambiguous at specificity 1.00 and the model disagrees (9 rows this month). **Do not quote the 60% as a head-to-head accuracy claim** — it is measured on the corrected subset, which is selected for model error by construction.
- [x] (T-67) **`close-cycle.sh finish` aborts if handed the run dir's own `reviewed.json`.** `cmd_finish` does `cp "$reviewed_json" "$run_dir/reviewed.json"`; a self-copy exits 1 and `set -euo pipefail` kills the script before `apply` runs. Hit naturally in s71 — the run dir is the obvious place to put the file. Harmless (it aborts before writing anything) but it fails on the most natural usage. Fix: skip the copy when source and destination resolve to the same path.
- [x] (T-68) **`close-cycle.sh prepare` prints a download path that does not exist.** It instructs `finish <run-dir> ~/Downloads/reviewed.json`, but there is no Linux browser on this machine (only `xdg-open`) and `/home/leandror/Downloads` is absent, so the review happens in Windows Chrome and the file lands elsewhere. Anyone following the printed line gets "no such reviewed file". Note s71 could NOT confirm where Chrome actually saves — `/mnt/c/Users/Leandro/Downloads` holds one unrelated installer — so the fix should print the file the user actually has rather than guess a directory: drop the path and say "the file your browser downloaded".
- [ ] (T-69) **`review` hard-errors when EVERY row was rejected** — `Error: no rows to review`, non-zero exit. Found in s71 by smoke-testing `close-cycle.sh` against an all-malformed batch, not by reasoning. A batch where nothing parsed is a legitimate state with an obvious correct outcome ("nothing to review — fix `failed.csv` and re-run"), not a fatal error; today it aborts `prepare` after `batch-auto` has already snapshotted, classified and written its CSVs. **It does not affect a normal close**, which always has reviewable rows, which is why it survived the first real close — but it makes the degenerate case the one that fails loudest, and it is the exact case someone re-running a repaired `failed.csv` can hit if their repairs are still wrong. Cousin of S1 (s68): that fixed "SOME rows unparsed kills the step"; this is "ALL rows unparsed kills the step". Note the error also dumps cobra's full usage text, which buries the real message. **Mitigated but not fixed in `close-cycle.sh`:** both assertions were moved ahead of the `review` call, because ordering them after it skipped the reject assertion in precisely the case it most needed to check.
- [ ] (T-70) **`auto_inserted` has TWO producers, kept equal only by a filter** — `writeClassifiedCSV` renders `fmt.Sprintf("%v", r.AutoInserted)` while `writeReviewCSV` hardcodes the literal `"false"`. They agree only because review.csv skips auto-inserted rows, so the hardcode is never wrong *today*. **No test distinguishes them either**: the s72 cross-writer tests run both writers through identical assertions, but every row they feed is `AutoInserted: false`, so the two spellings agree by construction. Relax that filter — or make review.csv carry an auto-inserted row for any reason — and the literal becomes a lie with nothing to catch it. This is the divergence-hiding shape T-54 came from. Not a bug today; noted in `internal/review/.memories/QUICK.md`. Fix = derive both cells from the same expression, or add a test that feeds an auto-inserted row through both writers.
- [ ] (T-71) **A backgrounded `generate_code` call silently ignores `output_file` and injects no verdict template** — measured twice in s72: the MCP call exceeded the foreground window, moved to a background task, returned its content in the completion notification, and wrote **nothing** to the requested path, with no warning. The `PostToolUse` verdict template is also skipped, so those verdicts survive only in the transcript unless written by hand. Cross-repo: `ollama-bridge` lives in `/mnt/i/workspaces/llm/`. **Also correct the s71 claim that `calls.jsonl` does not exist** — it is live at `~/.local/share/ollama-bridge/calls.jsonl` (908 entries at s72) and DOES record backgrounded calls; only the verdict is missing. See [[feedback_verdict_transcript_gap]].
- [x] (T-72) **`review` hard-errored on any BR-thousands value — one such row killed the whole review step.** Found and fixed in s73 while implementing T-64; verified through the REAL producer→consumer seam, not by reading: `writeClassifiedCSV` emits `1.234,56` and `review.ReadQueue` returned `line N: invalid value: invalid value format: 1.234,56`, a HARD error that aborts the entire step. **Reachable, not theoretical** — the value column deliberately keeps the RAW token so the installment count survives (s67), and `TestAdd_ReadsBrazilianThousandsAmount` already pins `1.234,56` as legal CLI input. **Root cause is the T-54 shape a third time:** one function, two callers, and a normalization step only one of them applied — `normalizeThousands` lived in `internal/parse`, while `ReadQueue` called `utils.ParseCurrencyWithInstallments` directly and so never stripped the dots. Both sides' own tests were green throughout. **Fix:** new `parse.Value(valueStr)`, the value-only door onto the boundary and the mirror of `parse.Date`; `Fields` DELEGATES to it (so normalization has exactly one call site — grep-verifiable) and `ReadQueue` calls it instead of utils. Design Q3 ("the boundary owns ALL input normalization") is what decided the placement over the alternative of pushing normalization down into `pkg/utils`. **Mutation-verified:** deleting the `normalizeThousands` call inside `Value` — a mutation that still COMPILES — turns exactly the two thousands tests red while both multiplier tests stay green, which is what proves `ReadQueue` really routes through the boundary. Note the corollary: the multiplier seam test is NOT sensitive to that routing (utils knows the notation either way), so **the thousands case is the only guard on it** — do not delete it as redundant.
- [ ] (T-73) **`classify` accepts neither thousands nor installment notation** — `classify.go:46` calls plain `utils.ParseCurrency(args[1])` rather than going through the parse boundary, so `classify "item" "1.234,56" ...` and `classify "item" "405,25 x4" ...` both fail while every other entry point accepts them. Noted in s73 during T-64 and deliberately kept out of that diff. Low harm (classify is read-only and prints a prediction) but it belongs with the T-41 boundary work rather than on its own. Fold into WS-E or a T-41 tidy pass. **CORRECTION (s73, advisor review): `classify` is NOT the last such caller, and the T-64 commit message's claim that `ParseCurrencyWithInstallments` has "two live callers" is wrong — there are THREE.** The third is `models.NewExpense` (`internal/models/expense.go:44`), reached from a path that is retired-but-not-deleted and therefore still **registered and reachable**: `batch` (`rootCmd.AddCommand(batchCmd)`, `batch.go:44`) → `batch.NewProcessor` → `internal/parser` → `models.NewExpense`. Verified by tracing the chain, not assumed from the WS-E label. **Consequence:** plain `batch` silently gained the multiplier notation on a path that writes DIRECTLY to the workbook, with no thousands normalization (that path never had any — pre-existing) and no test. The behavior is probably right — `batch` expands installments at insert time, so `405,25 x4` becomes 4 rows of 405,25 — but it is unexercised. **Deliberately not fixed:** adding tests or normalization to a path scheduled for deletion under WS-E is waste. WS-E must delete this caller; until it does, treat "the notation reaches the workbook through `batch` untested" as known.
- [ ] (T-74) **A dot-only value token is ambiguous and silently resolves 1000× wrong** — `normalizeThousands` strips dots only when a comma is ALSO present, so `1.234,56` → 1234.56 (right) but bare `1.234` → 1.234 (a thousandth of what a Brazilian writer means). **Pre-existing, not introduced by T-64** — the dot-only rule is deliberate legacy behavior recorded in `internal/parse/.memories/QUICK.md` ("dot-only tokens keep legacy decimal-dot meaning"), and it predates the multiplier. Filed in s73 because the multiplier makes it reachable in a new position (`1.234 x4` is now a parseable token that means 4 × R$1,23 rather than 4 × R$1.234,00). Deciding this needs a call on whether dot-as-decimal is still worth supporting at all, given the input source is human-typed PT-BR chat (vision Phase 4) where comma-decimal is universal.
- [x] (T-75) **Convert the Telegram chat export into a batch-auto CSV** — expenses are typed into a private Telegram group ("Gastos") and exported by hand as `ChatExport_<date>/result.json` (393 messages, 2025-05..2025-12 in the copy on disk). **Nothing in the repo reads that file.** The only trace is one archived line from s14 ("extracted 1601 expense entries from a large JSON file") — done ad hoc, never committed. This is the single manual link in an otherwise mature chain, and it is why only ONE month has been closed: everything downstream (`batch-auto` → `review` → `apply` → `generate-workbook`) is built, tested and measured, but a month costs whatever the hand-extraction costs. User confirmed ~7 months of 2026 (Feb–Aug) are sitting in the group unprocessed. **Also the right home for input validation:** all four rejects of the first close were typing errors — swapped fields (`Comida cinema;109,39;14/01`), a typo'd day (`201/01`), a stray `;` inside an item, and arithmetic that does not reconcile (299,00 + 49,90 ≠ 646,25) — none a classification problem. Catching those at the message, while the human still remembers the purchase, is strictly better than `failed.csv` months later, which is aimed at the latest possible moment to repair. Bounded and deterministic: no model, no workbook, testable against the real export already on disk. Step toward vision Phase 4 (`docs/expense-classifier-vision.md`), not a detour from it.
- [ ] (T-76) **Guard the new live-log id invariant** — since s74 every id in both logs hashes its own row, but NOTHING enforces it, so a future writer emitting a year-less date silently reintroduces the drift and the next reader inherits the same four-session confusion. Evidence it currently holds by luck rather than construction: it is an emergent property of T-41 being complete, confirmed only empirically (the s74 append came out canonical unaided, and every classification row written in 2026-08 carries a full date while all 649 written in 2026-06 do not). ⚠️ **Scope it to the LOGS AS DATA** — a `.claude/tools` check run at close time, NOT a unit test over `GenerateID`. "Every id equals the hash of its own row" is deliberately FALSE in `test/fixtures/apply-stale-id`, which exists precisely to prove a stored id may legitimately differ, so a test-level guard collides with the fixture corpus on its first run. Cheap: the check is ~15 lines and already written twice in the s74 transcript.
- [ ] (T-77) **Document the fixture id conventions (`type-routing-cycle`, `apply-basic`)** — the `reviewed.json` corpus carries THREE deliberate id conventions and only one is explained. `apply-join-id` and `apply-stale-id` have READMEs stating why their ids are what they are (and `apply-stale-id`'s warns against "fixing" it). `apply-basic` has none, and `type-routing-cycle` uses literal placeholders (`uber-1`, `dentista-ambiguous-1`) that are not valid id shapes at all — harmless ONLY because `apply` recomputes ids since T-41 slice 4, a property conferred by a later change that the fixture does not itself assert. Nothing distinguishes "deliberately fake" from "nobody computed the real value", which is the trap `apply-stale-id`'s README exists to prevent — recorded in three places precisely because someone would otherwise tidy it. **Cheap fix:** add READMEs. **Fuller fix:** replace the placeholders with genuine hashes, but that may WEAKEN incidental coverage, so mutation-check first (revert the id recompute and confirm the fixture still fails). Also relevant: a hand-authored shape no producer emits is the pattern s68 locked a decision against.
- [ ] (T-78) **Every installment series in the 2025 log is missing its tail rows.** Measured in s75 while validating the T-75 oracle, on data already on disk: **all 14** installment series typed into the Telegram group during 2025-05..12 appear in `expenses_log.jsonl` with **only their first installment**. Value of the absent installment rows: **R$ 7.309,43** against R$ 119.050,71 for the 499 rows in that window (R$ 6.589,86 in-window, R$ 719,57 landing in 2026). **The NET budget error is smaller: 3 of the 14 series carry a compensating same-day lump** (`Ração Orion Petz` 161,67 + 485,00; `Shopee <phone>` 648,33 + 3.890,00 — **REPAIRED s76, see below**; `Nefrologista Orion <person B>` 150,00 + 300,00), so those are **over**-counted by one installment each (R$ 960,00) while **11 of 14 are genuinely short by R$ 3.594,44** — net understatement about **R$ 2.634,44**. Two independent methods — multiset `(date,value)` residue and item-based series matching — agree **to the centavo, zero disagreeing series**. Report: `.claude/t75-oracle-viability.md` [ref:t75-oracle]. **This is the T-21 defect (s69) sitting at scale in the historical data** — the human doing by hand what the review route did silently until T-21 fixed it. **USER RULING (s75): "if an expense was reported as an installment, it should've been an installment"** — so these are omissions, not a bookkeeping convention, and the 2025 budget understates. ⚠️ **Conventions are genuinely mixed, and a blanket backfill would triple-count the three lump series** — expand ONLY the 11 verified short ones, and for the 3 lump series the repair is the opposite (drop the stray first-installment row, or drop the lump and expand). ⚠️ **Do not re-derive the lump set with a value-only match:** an exact-cents `per × N` lookup misses Xiaomi by two centavos (3890 ÷ 6 = 648,3333, so `per × N` = 3.889,98), and adding tolerance without requiring ITEM agreement returns 4, two of them coincidental collisions (*Consulta <person F>*, *Diarista <person D>*). Both mistakes were made in s75 before the number settled. **Not yet established:** whether 2022–2024 share the gap (untested; 5.R4 extracted them the same way, so likely), and whether the tail installments were actually paid — that is the user's knowledge and it gates any backfill. **Do NOT fold into T-75** (a capture tool) and do not backfill without re-reading the s74 log-migration discipline: verify with a check proven able to FAIL, rewrite surgically rather than re-serialising, and refuse to write anything on an ambiguous row. **PARTIALLY DONE s76 — 1 of the 3 lump series repaired, USER RULING option (a) "installments win", the user having spotted it in the regenerated 2025 workbook.** `Shopee <phone>`: dropped the 3.890,00 lump (`6dfc6bdb3ca9`) and replaced the bare first installment (`b46ebf255d4b`) with the SIX rows the CURRENT expander produces — **generated by calling `appender.ExpandAndAppend` itself, not hand-built**, so the ids, the `(i/N)` item suffix and the float are the code's own output. Now 24/06..24/11/2025 at 648.3333333333334, summing to exactly R$ 3.890,00. **The suffix is the tell that made the target unambiguous: all 45 `(i/N)` rows in the log are 2026 and none is 2025**, so the 2025 rows are pre-T-21 output and the post-T-21 form is what should have been written. **`classifications.jsonl` deliberately untouched** — `IPVA 2026` shows the convention: ONE classification row under the BASE item, N expense rows with suffixes and no classification, so `b46ebf255d4b` now living only in the classification log is exactly right. Verified: all 2220 ids hash to themselves (**and the check was proven able to fail — a one-centavo perturbation breaks it**), duplicate ids unchanged at 8, `diff` against the pre-surgery snapshot is exactly two hunks, `classifications.jsonl` sha256 unchanged, `workbook-2025.xlsx` regenerated with empty stderr. Backup: `logs-before-t78-xiaomi/` in the session scratchpad; script `t78_xiaomi_repair.py` beside it. **ALL THREE LUMP SERIES NOW REPAIRED (s76), same ruling, same method; 11 short ones still open.** `Ração Orion Petz` — message id 13502 said `485/3`; the lump came from the **5.R4 old-workbook extraction** (`ts 2026-06-20T13:48:55Z`, the 1717-row batch), as Xiaomi's did, so both were *the workbook disagreeing with the message* and the s75 ruling settles them. Now 31/05, **30/06** (the expander clamps 31 June → 30), 31/07/2025 at 161.66666666666666, total exactly R$ 485,00. `Nefrologista Orion <person B>` — **a different shape: BOTH rows came from the Telegram import**, because the human typed the expense twice in the chat (id 13962 `…; 11/12; 300/2` on 11/12, then id 13978 `nefrologista <person B> Orion; 11/12; 300,00` on 18/12, reworded, same date, same total). **USER RULING s76: installments.** Now 11/12/2025 + **11/01/2026** at 150,00. ⚠️ **The float test that identified the other two CANNOT work here** — `485/3` stores 161.66666666666666 and `3890/6` stores 648.3333333333334, repeating decimals that prove the expander divided, but `300/2` is exactly `150.0`, indistinguishable from a typed `150,00`; only the Telegram export settled it. ⚠️ **That case is LIVE, not historical:** both messages parse cleanly, so today's converter imports both — the same duplicate-capture gap as the `batch-auto` warn-and-append item. Verified across both repairs: all 2221 ids hash to themselves (**check proven able to fail**), duplicate ids unchanged at 8, `diff` is exactly four hunks, `classifications.jsonl` sha256 unchanged, **both** workbooks regenerated with empty stderr (2026 too — the Nefrologista tail lands in a closed month). Backups `logs-before-t78-xiaomi/` and `logs-before-t78-lumps2/`, script `t78_lump_repair.py`. **Checked and CLEARED as a false positive:** `entrada <redacted>` 52,50 + 420,00 on 24/02/2025 looks like an 8× lump (420 ÷ 52,50 = 8 exactly) but the log holds TEN monthly 420,00 rows — a recurring payment plus an unrelated item. That is precisely the coincidental collision this entry warns about, reproduced rather than newly found.
- [ ] (T-79) **`*` is a third spelling of the multiplier and is currently rejected.** **USER RULING (s75, 2026-09-02): `89,90*3` means 3 installments of 89,90 — identical to `89,90 x3`.** Found in the 2025 Telegram export (`Cerveja sadhu x3; 15/11; 89,90*3`), which `ParseCurrencyWithInstallments` rejects today because the mode switch is the presence of `x`/`X` only. **A claim made earlier in s75 that `*` should be REJECTED — "the notations are inverses, so guessing is wrong by 3×" — was wrong and is retracted:** that argument distinguishes `/` from `x`, and `*` cannot plausibly mean divide, so no ambiguity exists to guard against. **Preferred placement: widen the mode switch in `ParseCurrencyWithInstallments` (presence of `x`/`X`/`*`) rather than normalizing `*` inside the T-75 converter** — one notation vocabulary, and every entry point gains it instead of only the Telegram path. Small and self-contained, but it is T-64-family work: pin it in the SAME adjacent test block as the other two notations so the three can never be conflated, and mutation-verify (a mutation must still COMPILE to prove anything — s72). ⚠️ **Watch the reading resolution:** `*` joins a token space where `4x5` is already REJECTED as ambiguous, so confirm `4*5` behaves identically rather than accidentally resolving. ⚠️ **Note the quantity/installment distinction this surfaced, now settled:** the user's convention writes QUANTITY in the ITEM field (`Café lor x15` and `perlita shopee 25L x2` are both logged as ONE row, with `/3` in their value fields doing the installment work) and INSTALLMENTS in the VALUE field, so a value-field marker always means installments whatever its spelling. Do not teach the parser to read the item field. ⚠️ The `Cerveja sadhu` row itself was never entered in the workbook (no November 2025 row exists), so there is no historical precedent to read off — the ruling is the only source.
- [ ] (T-80) **`batch-auto` WARNS on a duplicate and appends it anyway — and an auto-appended row never reaches the review page.** Found s75 in the first close made from a Telegram export. The run printed `⚠  duplicate: "<redacted item>" already in expense log (id 4f1d167f9e55)` and wrote the row regardless; verified by reading the log, where that id then appeared **twice** with identical date and value (1 of 18 appended rows). **The two halves compound, which is what makes it worth fixing rather than documenting.** The warning goes only to stderr scrollback, AND the row classified at 95% so it was AUTO-appended before review — so the single place a human would catch it is the single place it does not appear. Because both logs are append-only, the only removal path is `close-cycle.sh restore`, i.e. rolling back the whole close; the decision therefore has to be taken BEFORE the expensive human review, not after it. ⚠️ **The fix is NOT "skip duplicates".** `GenerateID` hashes `item|date|value`, so two genuinely separate identical purchases collide by construction — s72's `San michel` ×3 and s74's 8 same-day duplicates are real, and the live log carried 8 repeated ids before any of this work. That is why the T-75 oracle matches on a MULTISET. **Candidates:** route a warned row to REVIEW instead of auto-appending it (the human then adjudicates in the one place they are already looking), or write the warning somewhere durable that `close-cycle.sh` can assert on. Handled by hand for the s75 close — logs restored, the one line excluded from `close-input.csv` with the reason recorded IN the file, re-run clean. **Related but distinct:** `Nefrologista Orion <person B>` (T-78) shows the same duplicate arriving from the SOURCE — two Telegram messages a week apart, both parsing cleanly — so `telegram-import` will import both today and the same gap catches it downstream.
- [ ] (T-81) **`[ref:bash-wrappers]` is DANGLING — CLAUDE.md's hard rule points at a block that does not exist.** Found s75 while running `check-ref-integrity.py` during the post-close doc audit. CLAUDE.md's Documentation Rules say *"New scripts/tools — add to `[ref:bash-wrappers]` in `.claude/index.md`"*, but no ref block for that key exists anywhere in the repo — no opening/closing HTML-comment marker pair is defined for it — so **the rule cannot be followed as written** and every new script silently skips it. The tag is referenced from CLAUDE.md, `.claude/tasks.md` and a dozen archived handoff reports. **Decide which:** create the block in `.claude/index.md` around the existing scripts/tools table (the table IS the content the rule wants, it just carries no marker), or retarget the rule at the table by name and drop the tag. ⚠️ `check-ref-integrity.py` currently exits **1** with 7 errors / 5 warnings, ALL pre-existing — the other six are `ref:judge-sees-the-change` and `ref:patterns-refactoring-characterize-first` (both resolve in the LLM repo, not here, so the checker's single-repo assumption may be the actual defect) plus duplicate blocks copied into `.claude/local/handoff-runs/*/report.md` snapshots. **Fix the whole exit-1 condition or the checker stays unusable as a gate** — it cannot currently distinguish a new breakage from the standing seven.
<!-- /ref:deferred -->
