# Real expense descriptions — never commit (sample is built from the gitignored corpus)
*.jsonl
*.bak
# The OOD probe set is synthetic and safe
!ood.jsonl
# Taxonomy variants derived from the personal config/taxonomy.json — never commit
taxonomy-*.json
# Run logs (item ids only, but ephemeral)
*.log
